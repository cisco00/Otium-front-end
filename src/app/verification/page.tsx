'use client';

import { RenterVerification } from "@/components/RenterVerification";

export default function VerificationsPage() {
    return (
        <div>
            <RenterVerification onNavigate={() => { }} />
        </div>
    );
}