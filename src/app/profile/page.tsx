'use client'

import { ProfileSettings } from "@/components/ProfileSettings";

export default function ProfilePage() {
    return (
        <div>
            <ProfileSettings onNavigate={() => { }} />
        </div>
    );
}