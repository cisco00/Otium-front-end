'use client';

import { MyBookings } from "@/components/MyBookings";

export default function BookingsPage() {
    return (
        <div>
            <MyBookings onNavigate={() => { }} />
        </div>
    );
}