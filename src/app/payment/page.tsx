'use client'

import { PaymentMethods } from "@/components/PaymentMethods";

export default function PaymentPage() {
    return (
        <div>
            <PaymentMethods onNavigate={() => { }} />
        </div>
    );
}